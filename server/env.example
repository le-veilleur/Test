# ============================================
# Configuration Backend (server/.env)
# ============================================
# Copiez ce fichier vers server/.env et remplissez les valeurs
# Commande: cp env.example server/.env

# Port du serveur backend
PORT=3000

# URL du backend (pour les webhooks)
# En production, utilisez votre URL publique (ex: https://api.votredomaine.com)
BACKEND_URL=http://localhost:3000

# URL du frontend (pour les redirections OAuth)
# En production, utilisez votre URL publique (ex: https://votredomaine.com)
FRONTEND_URL=http://localhost:5173

# Configuration Unipile
UNIPILE_BASE_URL=https://api25.unipile.com:15594
UNIPILE_API_KEY=wU+2vV8i.Rxs+W0qDKDh8ljO968bNZl4LVHHdxL/vBh7onWcE6iU=

# ============================================
# Configuration Frontend (client/.env)
# ============================================
# Copiez ce fichier vers client/.env et remplissez les valeurs
# Commande: cp env.example client/.env

# URL de l'API backend
# En production, utilisez votre URL backend publique
VITE_API_URL=http://localhost:3000
